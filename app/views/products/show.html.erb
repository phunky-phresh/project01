<style media="screen">
  .jumbotron {background-image: url(<%= @products.image %>); background-size: auto; background-repeat: no-repeat; background-position: top right}
</style>

<div class="jumbotron">
  <div class="col-md-4">
    <h2 class="display-4"><%= @products.name %></h2>
    <p class="lead">This is a simple hero unit, a simple jumbotron-style component for calling extra attention to featured content or information.</p>
    <hr class="my-4">
    <p>It uses utility classes for typography and spacing to space content out within the larger container.</p>
  </div>
  <div class="col-md-4">

  </div>
</div>



  <ul>
    <li>Shape: <%= link_to @products.shape.name, "/shapes/#{@products.shape.id}" %></li>
    <li>Profile: <%=link_to @products.profile.name, "/profiles/#{@products.profile.id}" %></li>
  </ul>
  <% @products.reviews.each do |review| %>
  <div class="review">
    <h3><%= review.title  %></h3>
    <ul>
      <li><%= review.date %></li>
      <li><%= review.rating %>/10</li>
    </ul>
    <p><%= review.brief %></p>
  </div>
  <% end %>
